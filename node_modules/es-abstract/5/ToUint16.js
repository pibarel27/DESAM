'use strict';

var abs = require('es-abstract/5/abs');
var floor = require('es-abstract/5/floor');
var modulo = require('es-abstract/5/modulo');
var ToNumber = require('es-abstract/5/ToNumber');

var $isNaN = require('es-abstract/helpers/isNaN');
var $isFinite = require('es-abstract/helpers/isFinite');
var $sign = require('es-abstract/helpers/sign');

// http://262.ecma-international.org/5.1/#sec-9.7

module.exports = function ToUint16(value) {
	var number = ToNumber(value);
	if ($isNaN(number) || number === 0 || !$isFinite(number)) { return 0; }
	var posInt = $sign(number) * floor(abs(number));
	return modulo(posInt, 0x10000);
};
